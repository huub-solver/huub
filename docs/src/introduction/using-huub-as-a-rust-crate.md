# Using Huub as a Rust crate

Huub can also be used as a Rust dependency:

```bash
cargo add huub
```

This mode is intended for deeper integration and programmatic control over
models and solving workflows.
