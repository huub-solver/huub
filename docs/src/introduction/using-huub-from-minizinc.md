# Using Huub from MiniZinc

Huub can be installed as a MiniZinc solver.

1. Download a Huub release for your platform.
2. Extract the release to a suitable location.
3. Add the `share/minizinc/solvers` directory to `MZN_SOLVER_PATH`.
4. Verify the installation with `minizinc --solvers`.
